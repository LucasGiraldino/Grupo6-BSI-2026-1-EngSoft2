# Database Configuration
POSTGRES_DB=sigaac
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123
POSTGRES_PORT=5432

# Application
APP_PORT=8080

# JWT Security
# Gere um valor seguro com: openssl rand -hex 32
JWT_SECRET=coloque-aqui-um-valor-longo-e-aleatorio

# (variáveis abaixo ainda não utilizadas, reservadas para expansão futura)
# API_SECURITY_TOKEN_ISSUER=sigaac
# API_SECURITY_TOKEN_EXPIRATION_HOURS=2
# API_SECURITY_TOKEN_REFRESH_EXPIRATION_HOURS=24
